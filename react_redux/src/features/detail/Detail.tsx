import React from "react";
import { PROPS_DETAIL } from "../types";

const Detail: React.FC<PROPS_DETAIL> = ({
  detailId,
  weight,
  times,
  userDetail,
  log,
}) => {
  return <div>ああああああ</div>;
};

export default Detail;
